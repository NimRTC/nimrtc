# NimRTC v0.10.2 — Release Notes

> Status: **Tech Preview** — no API stability commitment through v1.0.0.
> Tag: `v0.10.2` · Target date: 2026-09-17 · Owner: BDFL

## Summary

v0.10.2 is a **patch on the v0.10 line** that closes the
`docs/plan/transport-selection.md` §6.5 follow-up gap: it lifts the four
Transport PAL seams (DTLS / SCTP / raw_UDP / transport-stack) onto
typed registry hooks so they participate in the unified
`core::register_all_default_plugins()` entry point. No new protocol or
content features, no new third-party dependencies, no public API change
— `nimrtc/engine/engine.hpp` is **byte-identical** with v0.10.1. The
engine's DTLS lookup now goes through `reg.get_dtls_session("wolfssl")`
(was direct `make_unique<DtlsSessionWolfSSL>`), with the lookup id
hard-coded to keep the public surface frozen. Slice 7.5 will widen this
to a `cfg.dtls_name` field; `WebRtcClassicStackFactory` / `RawUdpArqStackFactory`
remain the Slice 7.5 deliverables that make the engine switch off the
shell `CapabilitySelectorStackFactory`.

## Highlights

1. **Four new typed registry hooks** (`core::PluginRegistry`):
   - `register_dtls_session(id, factory*)` / `get_dtls_session(id)`
   - `register_sctp_socket(id, factory*)` / `get_sctp_socket(id)`
   - `register_raw_udp_datagram(id, factory*)` / `get_raw_udp_datagram(id)`
   - `register_transport_stack(id, factory*)` / `get_transport_stack(id)`
   Each module's `register_default_plugins()` publishes its factory
   through the matching typed slot. All four slots participate in
   `core::register_all_default_plugins()` (PAL Slice 2/3 unified table).
2. **Engine DTLS lookup via registry** — `engine.cpp` resolves the DTLS
   factory through `reg.get_dtls_session("wolfssl")` (was direct
   `make_unique<DtlsSessionWolfSSL>`). `engine.hpp` is **byte-identical**
   with v0.10.1 — the lookup id is hard-coded to keep the public API
   surface frozen. Non-wolfSSL builds (defensive `else` branch) keep the
   pre-Slice-8 direct-construction path. Slice 7.5 will widen this to a
   `cfg.dtls_name` field.
3. **Callback types promoted to `plugins/base.hpp`** (§6.5). The local
   typedefs in `nimrtc/sctp/sctp_socket_iface.hpp`
   (`plugins::OnSctpRecvCb`) and `nimrtc/raw_udp/raw_udp_datagram.hpp`
   (`plugins::Endpoint`, `plugins::OnDatagramCb`) are now canonical in
   `plugins/base.hpp`. The Slice 5/6 headers no longer re-declare them
   — a single source of truth. Source-compat: any pre-Slice-8 caller
   that wrote `plugins::OnSctpRecvCb` etc. continues to compile.
4. **`IRawUdpFactory` interface added**
   (`src/raw_udp/include/nimrtc/raw_udp/raw_udp_factory_iface.hpp`).
   `ArqRawUdpFactory` now publicly inherits the interface, parallel to
   `dtls::IDtlsSessionFactory` and `sctp::ISctpSocketFactory`.
5. **Shell `CapabilitySelectorStackFactory`** (id="default") registered
   through the new `register_transport_stack()` hook. The factory
   produces a `CapabilitySelectorStack` whose component accessors
   return references to null-instance singletons (no-op / kErrNotReady).
   Slice 7.5 will replace this with `WebRtcClassicStackFactory` (real
   ICE+DTLS+RTP+SCTP composition).

## Added

- **`nimrtc::transport::register_default_plugins()`** — Slice 8 entry
  point that publishes `CapabilitySelectorStackFactory` into the
  `core::PluginRegistry::register_transport_stack()` slot.
- **`NIMRTC_REGISTER_DTLS_SESSION / NIMRTC_REGISTER_SCTP_SOCKET /
  NIMRTC_REGISTER_RAW_UDP_DATAGRAM / NIMRTC_REGISTER_TRANSPORT_STACK`**
  macros — convenience `static ::nimrtc::core::detail::Registrar`
  wrappers for the four new typed slots, parallel to the existing
  audio / video / bwe / scheduler macros.
- **`IRawUdpFactory` interface** — typed factory contract for
  `IRawUdpDatagram` backends (Slice 6.5 → Slice 8 promotion).

## Changed

- **`dtls::register_default_plugins()`** now publishes
  `WolfsslDtlsFactory` through `register_dtls_session("wolfssl")`
  (Slice 8). Legacy `dtls::test_only::get_wolfssl_factory()` is
  preserved as a back-compat accessor that now reads back through
  the registry so the typed slot is the single source of truth.
- **`sctp::register_default_plugins()`** now publishes
  `SctpStubFactory` through `register_sctp_socket("stub")` (Slice 8).
  Legacy `sctp::test_only::get_stub_factory()` is preserved similarly.
- **`raw_udp::register_default_plugins()`** now publishes
  `ArqRawUdpFactory` through `register_raw_udp_datagram("arq")`.
- **`pal_default_registrars.cpp` `kDefaultRegistrars[]`** extended
  with DTLS / SCTP / raw_UDP / transport entries — every transport-
  layer seam now participates in the unified entry point.
- **Engine `init_modules_once()` DTLS construction** now goes through
  the registry (see Highlights).

## Test coverage

- 7 baseline `EnginePluginLoading` subtests (Slice 1 — transport / audio3a /
  RTP / SDP / JB presence; idempotency; engine open path) stay green.
- 2 PAL Slice 2 / 3 subtests (`pal_default_registrars_table_is_nonempty`,
  `pal_plugin_id_wrappers_preserve_string_values`) stay green.
- **5 new Slice 8 subtests** (`slice8_dtls_session_registry_hook_populated`,
  `slice8_sctp_socket_registry_hook_populated`,
  `slice8_raw_udp_datagram_registry_hook_populated`,
  `slice8_transport_stack_registry_hook_populated`,
  `slice8_idempotent_register_does_not_duplicate`) verify the four new
  hooks are populated and that `register_all_default_plugins()` remains
  idempotent under repeated calls.

Total: **15 / 15** subtests in `test_engine_plugin_loading`. Full suite
**389 / 389** (`ctest -C Debug`), 0 failures; 2 `Audio3ATapFixture`
subtests intentionally disabled (pre-existing condition, unrelated to
v0.10.2).

## Notes

- No public API change — `nimrtc/engine/engine.hpp` is byte-identical
  with v0.10.1.
- `git log v0.10.1..v0.10.2 --oneline` shows the Slice 8 commit plus
  the release artifacts (VERSION bump, this file).
- The four new DTLS / SCTP / raw_UDP / transport-stack registry hooks
  close the §6.5 follow-up gap listed in
  `docs/plan/transport-selection.md` (was: "Slice 8 必须顺手补").
- **Deferred to v0.10.x / Slice 7.5**:
  - `WebRtcClassicStackFactory` — real ICE + DTLS + RTP + SCTP
    composition; the Slice 7.5 deliverable that lets the engine switch
    off `CapabilitySelectorStackFactory` (id "default").
  - `RawUdpArqStackFactory` — ARQ-UDP control stack composition
    (Slice 5.3 rule 1 of `docs/plan/transport-selection.md`).
  - `EngineConfig::dtls_name` field — widens the hard-coded
    `"wolfssl"` lookup to a Profile-driven id.
- **Deferred to v0.11.0** (P2 kickoff):
  - `UsrsctpSocketFactory` (id `"usrsctp"`) — DataChannel interop.
- 国密后端 (SM2/SM4) remains P4 / Enterprise — not in v0.10.2.
- API still **experimental / not for production** through v1.0.0.
- No binary artefacts shipped (源码为主).
