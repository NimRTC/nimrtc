# NimRTC v0.12.0 — Release Notes

> Status: **Tech Preview** — no API stability commitment through v1.0.0.
> Tag: `v0.12.0` · Target date: TBD · Owner: BDFL

## Summary

v0.12.0 is a **platform expansion + GMSSL TLCP DTLS backend** milestone
on the v0.11.0 line:

1. **GMSSL TLCP DTLS backend（ADR-013）** — `DtlsSessionGmSSL`
   wraps GMSSL v3.x's GM/T 0044 (TLCP) handshake + SM2/SM3/SM4
   certificates + SM4-GCM encryption. Registered under id `"gmssl"` in
   `core::PluginRegistry::register_dtls_session()`. Engine default
   remains wolfSSL (zero regression); OHOS profile defaults to GMSSL.
   ADR-013 documents the decision rationale (GM/T 0044 TLCP over
   deprecated GM/T 0024; GMSSL not vendored; default cipher suite
   preserved for browser interop).
2. **OpenHarmony aarch64 CMake toolchain（Stage 0 ✅）** — Stage 0 of
   `docs/plan/openharmony-support.md`: `cmake/toolchains/ohos-aarch64.cmake`
   (dual-mode: real OHOS SDK cross clang + `--target` fallback),
   six OHOS CMakePresets.json presets (3 configure + 2 build + 1 test),
   and `cmake/NimRTCOptions.cmake` `if(OHOS)` musl-compat branch.
   Stages 1–4 remain in-progress; see §6.
3. **`nimrtc_link_gmssl()` CMake helper** — probes GMSSL v3.x
   `<gmssl/tls.h>` + `libgmssl` in `/usr/local`, `/usr`, and
   `$ENV{GMSSL_ROOT}`; does not depend on OpenSSL (GMSSL ships its
   own SM2/SM3/SM4 crypto primitives).

Net source diff vs v0.11.0: ~1 800 lines across `src/dtls/`,
`cmake/`, `tests/`, `docs/`. Net new source files: 9
(`dtls_gmssl_session.cpp`, `dtls_gmssl_factory.cpp`, etc.).

## Highlights

1. **`DtlsSessionGmSSL`** — `IDtlsSession` implementation using GMSSL
   v3.x TLCP (GM/T 0044, TLS 1.3-based). ~900 lines.
   Supports ECDSA P-256 + AES-GCM by default (matches browser suite);
   SM2/SM3/SM4 suites available by editing `cipher_priority` table.
2. **`GmSSLDtlsFactory`** — registers as id `"gmssl"`. Test-only accessor
   `get_gmssl_factory()` parallels `get_wolfssl_factory()`.
3. **`NIMRTC_ENABLE_DTLS_GMSSL` CMake option** — OFF by default.
   When ON: builds `dtls_gmssl_session.cpp` + `dtls_gmssl_factory.cpp`,
   defines `NIMRTC_HAS_DTLS_GMSSL=1`, links GMSSL.
4. **OpenHarmony aarch64 toolchain** — dual-mode
   `cmake/toolchains/ohos-aarch64.cmake`: OHOS SDK clang cross-compile
   (Mode A, recommended) + `--target=aarch64-linux-ohos` clang fallback
   (Mode B, for sanity checks without SDK). Six OHOS presets in
   `CMakePresets.json`.
5. **`docs/plan/ohos-audio-hal.md`** — Stage 4 output: OHAudio
   selected as the audio HAL for OHOS; OpenSL ES rejected (deprecated);
   integration plan for `OHAudioSource` / `OHAudioSink` adapter classes
   documented. Implementation is a v0.12.x patch follow-up.
6. **New documentation** — `docs/guides/building-on-openharmony.md`,
   `docs/guides/switching-dtls-backend.md`,
   `docs/api/engine_api.md`, `docs/api/engine_config.md`,
   `docs/plugin_author_guide.md`, `docs/troubleshooting.md`,
   `examples/demo-agent-gateway/README.md`.

## Decision log

| Date | Decision | Rationale | Owner |
|---|---|---|---|
| 2026-09-21 | **GM/T 0044 (TLCP) selected over GM/T 0024; ADR-013 Accepted** | GM/T 0044 is the current standard; GM/T 0024 is deprecated; ADR-013 documents alternatives | BDFL |
| 2026-09-21 | **OHOS profile defaults to GMSSL; other 4 platforms remain wolfSSL** | Platform differentiation + no regression for existing users | BDFL |
| 2026-09-22 | **OHAudio selected as OHOS audio HAL; OpenSL ES rejected** | OHAudio is OHOS 9+ first-class NDK API; OpenSL ES deprecated; AAudio not available on OHOS | BDFL |
| 2026-09-22 | **Audio HAL implementation deferred to v0.12.x patch** | Controls v0.12.0 scope; OHAudio HAL research + decision document is the v0.12.0 deliverable | BDFL |

## Cross-references — where to look

- Architecture: `docs/zh/architecture.md` §10 (国密与信创), §13 (phase outline P4).
- OpenHarmony support plan: `docs/plan/openharmony-support.md` (Stage 0 ✅ / Stage 1 🔶 / Stage 2 ✅ / Stage 3 🔶 / Stage 4 ✅).
- OHAudio HAL decision: `docs/plan/ohos-audio-hal.md`.
- GMSSL backend ADR: `docs/adr/ADR-013-gmssl-dtls-backend.md`.
- OHOS build guide: `docs/guides/building-on-openharmony.md`.
- DTLS backend switching guide: `docs/guides/switching-dtls-backend.md`.
- Engine API reference: `docs/api/engine_api.md`.
- Engine config reference: `docs/api/engine_config.md`.
- Plugin author guide: `docs/plugin_author_guide.md`.
- Troubleshooting: `docs/troubleshooting.md`.
- Previous release notes on the same train:
  `docs/plan/v0.10.0-release-notes.md`,
  `docs/plan/v0.10.2-release-notes.md`,
  `docs/plan/v0.10.3-release-notes.md`,
  `docs/plan/v0.11-plan.md` (v0.11.0 plan).

## Platform support matrix

| Platform | Toolchain | v0.12.0 | Notes |
|---|---|---|---|
| Windows 10/11 | MSVC 19.43+ | ✅ | wolfSSL default |
| Linux x86_64 | GCC 11+ / Clang 12+ | ✅ | wolfSSL default |
| macOS arm64 | Apple Clang 15+ | ✅ | wolfSSL default |
| Linux aarch64 | GCC 11+ / Clang 12+ | ✅ | compile-green; deployment validate separately |
| OpenHarmony aarch64 | OHOS SDK clang 14+ | 🔶 | Stage 0 ✅ (toolchain + preset); Stage 1 🔶 (Linux + OHOS SDK validation pending); GMSSL default |

> **GM/T 密码**: OpenHarmony profile defaults to `dtls_name = "gmssl"`.
> All other platforms default to wolfSSL. Switch at build time with
> `NIMRTC_ENABLE_DTLS_GMSSL=ON` or at runtime with
> `EngineConfig::dtls_name = "gmssl"`.

## What's NOT in v0.12.0 (explicitly deferred)

- **OHOS-aarch64 end-to-end audio capture** — OHAudio adapter
  implementation is a v0.12.x patch (Stage 4 output is this document).
- **OHOS ↔ Chrome / Firefox interop** — TLCP is not supported by any
  browser; OHOS ↔ NimRTC-Desktop uses wolfSSL path (set OHOS side
  `cfg.dtls_name = "wolfssl"` for browser interop).
- **GMSSL SRTP backend** — GM/T 0044 SRTP profile is P4; SRTP
  continues to use wolfSSL.
- **usrsctp on OHOS** — deferred to v0.12.x patch after 4-platform
  green; OHOS uses `SctpStubFactory` (id `"stub"`) for now.
- **T-PAL Slice 6** (Raw UDP bypass) — v0.11.x patch.
- **T-PAL Slice 7** (Stack + Selector + Profile transport v1.1) —
  v0.12.0.
- **SFU-1 / DC-3 / BENCH-1** — pre-1.0 decision window per
  `docs/plan/v0.11-plan.md` §2.2.

## Compatibility / upgrade notes

- **Public API**: byte-identical with v0.11.0 except one additive field:
  `EngineConfig::dtls_name` (string, defaults to `""` → `"wolfssl"`).
  Any v0.11.0 caller that does not set `dtls_name` continues to compile
  and behave identically.
- **ABI**: Not committed; experimental through v1.0.0. Pin to a specific
  commit.
- **GMSSL builds**: `NIMRTC_ENABLE_DTLS_GMSSL=ON` requires GMSSL v3.x
  headers and library on the build host. Linux x86_64 and OHOS-aarch64
  are the supported hosts. Windows MSVC GMSSL builds are blocked by
  lack of pre-built GMSSL cmake targets on Windows; use Linux for
  GMSSL development.
- **No new third-party dependencies** for the default (wolfSSL) build.
- **No new tooling required.**

## Verification gate

| Gate | Description | Status | Notes |
|---|---|---|---|
| 1 | Four-platform CI (windows / linux-gcc / macos-clang / linux-aarch64) green | 🔶 linux-aarch64 TEMP-DISABLED (GitHub runner pool) | Windows + Linux x86_64 + macOS ✅ |
| 2 | `ctest --preset tests.msvc` on Windows | ✅ | Local verification |
| 3 | `ctest --preset tests` on Linux x86_64 | ✅ | Local verification |
| 4 | `test_dtls_gmssl_tlcp` on Linux x86_64 (GMSSL) | 🔶 Requires Linux + GMSSL env | Local verification gate; not automated in CI |
| 5 | OH-DOD-0: `cmake --preset debug.ohos-arm64` configure zero error (OHOS SDK installed) | 🔶 OHOS SDK not in local env | Stage 1 forward progress; documented in `openharmony-support.md` §6 |
| 6 | OH-DOD-1: `cmake --build --preset debug.ohos-arm64 --target nimrtc_engine` success | 🔶 OHOS SDK not in local env | Stage 1 |
| 7 | OH-DOD-4: `test_dtls_gmssl_tlcp` PASS on Linux x86_64 | 🔶 Requires Linux + GMSSL | Stage 2 |
| 8 | OH-DOD-6: `ohos-arm64` CI job in `ci.yml`，接通 | ✅ | Stage 3; `if: false` / `continue-on-error` |
| 9 | OH-DOD-7: README.md platform matrix updated + CHANGELOG.md v0.12.0 entry | ✅ | Stage 3 |
| 10 | OH-DOD-8: ADR-013 reviewed | ✅ | Stage 2 |
| 11 | OH-DOD-9: `docs/plan/ohos-audio-hal.md` reviewed | ✅ | Stage 4 |
| 12 | OH-DOD-10: v0.11.0 回归 — 4-platform build + ctest 全部维持绿色 | 🔶 linux-aarch64 TEMP-DISABLED | Windows + Linux x86_64 + macOS ✅ |

## Release manager checklist

- [ ] `git diff v0.11.0..HEAD -- CHANGELOG.md` reviewed (v0.12.0 entry
      complete and accurate)
- [ ] `git diff v0.11.0..HEAD --oneline` ≤ ~25 commits (GMSSL backend
      + OHOS toolchain + docs)
- [ ] `ctest --preset tests.msvc` green (Windows MSVC)
- [ ] `ctest --preset tests` green (Linux x86_64)
- [ ] `ctest --preset tests.macos` green (macOS arm64)
- [ ] `test_dtls_gmssl_tlcp` PASS on Linux x86_64 + GMSSL env
      (manual verification gate, not CI)
- [ ] `cmake --preset debug.ohos-arm64` zero error (Linux + OHOS SDK,
      manual gate; CI job is `if: false`)
- [ ] Four-platform CI green on `ci.yml` + `interop.yml`
      (linux-aarch64 job TEMP-DISABLED; windows + linux-gcc + macos-clang
      must be green)
- [ ] ADR-013 `docs/adr/ADR-013-gmssl-dtls-backend.md` Status: Accepted
- [ ] `docs/plan/ohos-audio-hal.md` reviewed and merged
- [ ] All new documentation files reviewed:
      `docs/guides/building-on-openharmony.md`,
      `docs/guides/switching-dtls-backend.md`,
      `docs/api/engine_api.md`, `docs/api/engine_config.md`,
      `docs/plugin_author_guide.md`, `docs/troubleshooting.md`,
      `examples/demo-agent-gateway/README.md`
- [ ] `git tag -s v0.12.0 -m "..."` (GPG-signed)
- [ ] `git push origin v0.12.0` triggers `release.yml` draft;
      BDFL reviews + publishes

## Provenance

- GMSSL backend commit: `dtls_gmssl_session.cpp` (~900 LOC) +
  `dtls_gmssl_factory.cpp` + `dtls_plugin.cpp` conditional registration
  + `CMakeLists.txt` option + `cmake/NimRTCVendored.cmake` helper
- OHOS toolchain commit: `cmake/toolchains/ohos-aarch64.cmake` +
  `CMakePresets.json` 6 OHOS presets + `cmake/NimRTCOptions.cmake`
  `if(OHOS)` branch
- ADR-013 commit: `docs/adr/ADR-013-gmssl-dtls-backend.md` (Status:
  Accepted, 2026-09-21)
- OHAudio HAL decision: `docs/plan/ohos-audio-hal.md` (Status:
  DECIDED, 2026-09-22)
- v0.11.0 first-release: 2026-09-XX
